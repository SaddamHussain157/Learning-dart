import 'loopsPractice.dart';

void main(List<String> args) {
  List name = [1, 2, 3, 4, 5, 's', true];
  // the upper example given dynamic List which means anything can be store
  // print(name);
  // name.add('Mirza khan');
  // print(name);
  // name.addAll([1, 2, 3, 4, 5, 5, 6, 6, 7, 7, 8]);
  // print(name);
  // List<int> even = [2, 4, 8, 10];
  // List<int> odd = [1, 3, 5, 7, 9];
  // List <int> merge = even + odd;
  // print('merge list: $merge');
  // if (merge % 2 == 0) {
  //   print(object)
  // }
  // List merge = ['even', ...odd, 'odd', ...even];
  // print(merge);
  // name.remove;
  // name.clear();
  // print(name);
  name.forEach((item) {
    print(item);
  });

  // print('ODD list: $odd and EVEN List $even');
}
